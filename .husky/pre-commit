# Skip lint-staged when only next-env.d.ts is staged
STAGED_FILES=$(git diff --cached --name-only)
STAGED_COUNT=$(echo "$STAGED_FILES" | wc -l)
if [ "$STAGED_COUNT" -eq 1 ] && [ "$STAGED_FILES" = "next-env.d.ts" ]; then
  exit 0
fi

npx lint-staged
